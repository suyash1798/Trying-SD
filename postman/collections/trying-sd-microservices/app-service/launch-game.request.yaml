$kind: http-request
name: Launch Game
method: POST
url: "{{appServiceUrl}}/games/:gameId/launch"
order: 2000
pathVariables:
  gameId: "{{gameId}}"
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "deviceId": "{{deviceId}}"
    }
